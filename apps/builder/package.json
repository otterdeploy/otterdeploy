{
  "name": "builder",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --env-file ../../.env --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc -b --checkers 4"
  },
  "dependencies": {
    "@otterdeploy/api": "workspace:*",
    "@otterdeploy/db": "workspace:*",
    "@otterdeploy/env": "workspace:*",
    "@otterdeploy/jobs": "workspace:*",
    "@otterdeploy/shared": "workspace:*",
    "better-result": "catalog:",
    "bullmq": "^5.65.0",
    "drizzle-orm": "catalog:",
    "evlog": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@otterdeploy/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
