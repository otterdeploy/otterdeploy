{
  "name": "builder",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "check-types": "tsc -b",
    "dev": "bun run --env-file ../../.env --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@otterstack/api": "workspace:*",
    "@otterstack/db": "workspace:*",
    "@otterstack/env": "workspace:*",
    "@otterstack/jobs": "workspace:*",
    "@otterstack/shared": "workspace:*",
    "bullmq": "^5.65.0",
    "drizzle-orm": "catalog:",
    "evlog": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@otterstack/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
