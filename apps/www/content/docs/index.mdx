---
title: Introduction
description: "What Otterdeploy is, what it runs, and how these docs are organised: start here for the model, the guides for the day to day, reference for the details."
icon: Album
---

Otterdeploy is a self-hostable deployment platform. You bring a Linux box, or
a cluster of them, and it gives you git-sourced builds, managed databases, a
Caddy edge with automatic HTTPS, per-pull-request previews, live logs, metrics
and scheduled backups. Everything runs on your hardware; nothing phones home,
and nothing stops working if we do.

<Callout type="warn" title="Under active development">
  Interfaces and schemas still change without migration paths. Run it on
  something you'd be willing to rebuild, and read
  [what isn't built yet](/docs/start/concepts#what-isnt-built-yet) before you
  plan around a feature.
</Callout>

## What you can run

- **Services**: apps built from a git repo (framework auto-detected, or your
  own Dockerfile), from a pre-built image, or from an upload.
- **Databases**: PostgreSQL, Redis, MariaDB, MongoDB and ClickHouse,
  provisioned as first-class resources next to the services that use them.
- **Compose stacks**: a whole `docker-compose` project as one resource, pasted
  inline or pulled from a repo.

Everything is reachable three ways: the dashboard, the `otterdeploy` CLI, and
the typed API the other two are built on.

## Where to start

<Cards>
  <Card
    title="Concepts"
    href="/docs/start/concepts"
    description="Organisations, projects, resources, deployments: the model everything else assumes."
  />
  <Card
    title="Install"
    href="/docs/start/install"
    description="Put the control plane on your own server."
  />
  <Card
    title="First deploy"
    href="/docs/start/first-deploy"
    description="From an empty project to a running service."
  />
  <Card
    title="CLI reference"
    href="/docs/cli/commands"
    description="Every command and flag the binary accepts."
  />
</Cards>

## Guides

Task-shaped pages for the things you'll do after the first deploy:
[services](/docs/guides/services), [databases](/docs/guides/databases),
[domains and TLS](/docs/guides/domains),
[environment variables](/docs/guides/environment),
[previews](/docs/guides/previews), [backups](/docs/guides/backups) and
[servers](/docs/guides/servers).

## Reference

The [manifest schema](/docs/reference/manifest) for `otterdeploy.json`, the
[CLI reference](/docs/cli/commands), and the
[HTTP API](/docs/reference/api) generated from the live OpenAPI spec.
