# build output
dist
.output
.nitro
.tanstack

# cloudflare — .wrangler holds the local dev state and the generated
# deploy/config.json that points wrangler at .output/server/wrangler.json
.wrangler

# fumadocs generated source
.source

# generated route tree
src/routeTree.gen.ts

node_modules
*.log
