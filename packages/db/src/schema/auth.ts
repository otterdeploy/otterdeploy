// Better Auth will generate these tables via its Drizzle adapter.
// We re-export them here so other schema files can reference them.
// The actual table definitions are created by: bunx @better-auth/cli generate
// For now, this is a placeholder — Better Auth creates the tables on first run.
export {};
