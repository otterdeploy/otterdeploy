{
  "name": "@otterdeploy/domain",
  "private": true,
  "type": "module",
  "exports": {
    ".": { "default": "./src/index.ts" },
    "./pipeline": { "default": "./src/pipeline/index.ts" },
    "./*": { "default": "./src/*.ts" }
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "better-result": "catalog:",
    "@otterdeploy/db": "workspace:*",
    "@otterdeploy/utils": "workspace:*",
    "@otterdeploy/docker": "workspace:*",
    "@otterdeploy/events": "workspace:*",
    "@otterdeploy/logger": "workspace:*",
    "@otterdeploy/secrets": "workspace:*"
  },
  "devDependencies": {
    "@otterdeploy/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^4.0.18"
  }
}
