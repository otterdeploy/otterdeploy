Rule: react-doctor/js-hoist-intl
Severity: warning
Category: Performance
Count: 1

This is slow because new Intl.NumberFormat() rebuilds on every call inside a function, so move it to the top of the file, or wrap it in useMemo

Suggestion: Move `new Intl.NumberFormat(...)` to the top of the file or wrap it in `useMemo`. Building one is slow, so don't redo it on every call

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/js-hoist-intl

Files:
  src/features/resources/components/postgres/tabs/data/studio-table-view.tsx:117
