Rule: react-doctor/prefer-module-scope-pure-function
Severity: warning
Category: Maintainability
Count: 10

`start` inside `SocialSignIn` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`invalidateList` inside `InstallationActions` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`onRemove` inside `GuestsSection` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`invalidate` inside `KeyCard` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`execInto` inside `ContainersTable` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`tone` inside `LiveHealthCell` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`copyLines` inside `RouteComponent` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`peak` inside `PlatformBody` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`recheck` inside `RouteComponent` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

`toggleSub` inside `RouteComponent` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.

Suggestion: Move the function above the component, at the top of the file. It doesn't use local state, so rebuilding it each update is wasted work.

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-module-scope-pure-function

Files:
  src/features/auth/components/social-sign-in.tsx:33
  src/features/git-providers/installation-actions.tsx:27
  src/features/projects/components/networking/route-access-guests.tsx:100
  src/features/ssh-keys/key-card.tsx:50
  src/routes/_app/$orgSlug/-components/docker-table-containers.tsx:40
  src/routes/_app/$orgSlug/-components/servers-live-cell.tsx:15
  src/routes/_app/$orgSlug/_shell/$projectSlug/logs.tsx:129
  src/routes/_app/$orgSlug/_shell/platform.tsx:76
  src/routes/_app/$orgSlug/settings/workspace/certificates.tsx:59
  src/routes/_app/$orgSlug/settings/workspace/notifications.tsx:119
